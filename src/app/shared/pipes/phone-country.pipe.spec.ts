import { PhoneCountryPipe } from './phone-country.pipe';

describe('PhoneCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
